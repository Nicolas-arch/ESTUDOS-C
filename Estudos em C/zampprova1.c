#include <stdlib.h>
#include <stdio.h>
#include <locale.h>
#define max 15


void teste(int a[], int *res)
{
    int i=0,j=0;

    for(i=0;i<max;i++)//preencher vetor
    {
        printf("Qual é o valor do %d saláraio: ",i);
        scanf("%f",&a[i]);
    }

    for(i=0;i<max;i++)
    {
        if((a[max]%5==0) && (a[max]%7==0))
    }

}

int main()
{

}
